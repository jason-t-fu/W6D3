
<button type="button" 
        class="follow-toggle" 
        data-username="<%= current_user.username %>"
        data-user-id="<%= user.id %>"
        data-initial-follow-state="<%= 
          current_user.follows?(user) ? "followed" : "unfollowed" %>"></button>
